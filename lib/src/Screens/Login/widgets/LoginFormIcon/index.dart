import 'package:flutter/material.dart';

class LoginFormIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image(
        image: AssetImage('assets/images/game-tv.png'),
      ),
    );
  }
}
